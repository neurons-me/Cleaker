import{_ as t,o as a,c as o,a0 as s}from"./chunks/framework.xN4chThJ.js";const m=JSON.parse('{"title":"Namespace Is Context","description":"","frontmatter":{},"headers":[],"relativePath":"Namespace-Is-Context.md","filePath":"Namespace-Is-Context.md"}'),n={name:"Namespace-Is-Context.md"};function c(r,e,i,d,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="namespace-is-context" tabindex="-1">Namespace Is Context <a class="header-anchor" href="#namespace-is-context" aria-label="Permalink to &quot;Namespace Is Context&quot;">​</a></h1><p><strong>neurons.me / suiGn</strong><strong>Status:</strong> Canon. Sharpens <a href="./The-Model.html">The Model</a>&#39;s <code>space</code>/<code>namespace</code> distinction and <a href="./NRP-Namespaces.html">NRP-Namespaces.md</a>&#39;s grammar with one rule that was previously implicit and inconsistently applied: <strong>a host is never, by itself, a namespace.</strong><strong>License:</strong> CC0 1.0 Universal — Public Domain</p><hr><h2 id="_0-the-one-sentence-version" tabindex="-1">0. The one-sentence version <a class="header-anchor" href="#_0-the-one-sentence-version" aria-label="Permalink to &quot;0. The one-sentence version&quot;">​</a></h2><p>A namespace is a <em>designated</em> context — a ledger of paths and references — and a host is only ever the physical surface a request happened to arrive through; conflating the two lets every machine that ever runs a monad silently mint its own namespace, which is the opposite of what <code>cleaker()</code> — &quot;fixes the current namespace context&quot; — is for.</p><h2 id="_1-four-terms-not-two" tabindex="-1">1. Four terms, not two <a class="header-anchor" href="#_1-four-terms-not-two" aria-label="Permalink to &quot;1. Four terms, not two&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Term</th><th>What it is</th><th>Example</th></tr></thead><tbody><tr><td><strong>host</strong></td><td>Physical surface / transport. Where a request enters.</td><td><code>local.host</code>, <code>suis-macbook-air.local</code>, <code>127.0.0.1</code></td></tr><tr><td><strong>namespace</strong></td><td>Semantic context. A ledger — an addressable index of paths and references.</td><td><code>cleaker.me</code>, <code>local.cleaker</code>, <code>jabellae.cleaker.me</code></td></tr><tr><td><strong>path</strong></td><td>An address inside a namespace.</td><td><code>hosts.suis-macbook-air.photos</code>, <code>groups.family.member.jabellae</code></td></tr><tr><td><strong>value</strong></td><td>What lives at a path: data, a claim, a derivation, or a pointer.</td><td><code>{ __ptr: &quot;...&quot; }</code>, a profile field, a claim record</td></tr></tbody></table><p><code>.</code> composes <strong>within</strong> a namespace — real membership (<code>jabellae.cleaker.me</code> is one identity inside <code>cleaker.me</code>&#39;s tree). <code>/</code> connects <strong>across</strong> host, surface, app, and path — a relation, never membership. <code>jabellae.suis-macbook-air.local</code> reads as valid <code>.</code>-composition but is a category error: it dot-joins a subject onto a <em>host</em>, not a namespace. The correct shape for &quot;who is this, at this physical machine&quot; is <code>&lt;host&gt;/@&lt;user&gt;</code> (a relation) — and that relation, alone, resolves to nothing: there is no ledger at a bare host to write it into. See §3.</p><h2 id="_2-namespaces-are-designated-not-derived" tabindex="-1">2. Namespaces are designated, not derived <a class="header-anchor" href="#_2-namespaces-are-designated-not-derived" aria-label="Permalink to &quot;2. Namespaces are designated, not derived&quot;">​</a></h2><p>The bug this doc closes: <code>getRootNamespace()</code> / <code>normalizeNamespaceRootName()</code> / <code>resolveNamespace()</code> (in <code>modules/monad/Typescript/src/</code>) currently derive a monad&#39;s &quot;root namespace&quot; directly from whatever physical hostname it happens to run on (<code>ME_NAMESPACE</code> / the <code>Host</code> header). That makes every host that ever runs a monad into a new, ad-hoc namespace — the exact contamination <code>local.host</code> was carved out to avoid (<code>local.host</code> is deliberately <em>not</em> <code>local.me</code>, precisely so a general-purpose local surface doesn&#39;t read as a namespace of its own).</p><p>A namespace is not &quot;whatever host is answering right now.&quot; It is a curated, designated context — <code>cleaker.me</code> (public), <code>local.cleaker</code> (local-scope), <code>jabellae.cleaker.me</code> (one identity&#39;s own context), <code>mexicoencuesta.com.mx</code> (a third-party designated space). A host can <em>serve</em> one or many of these. It never <em>is</em> one by default.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>host: local.host                          ← surface, not namespace</span></span>
<span class="line"><span>app: photos                                ← generic view, parameterized below</span></span>
<span class="line"><span>namespace: jabellae.cleaker.me             ← context being read</span></span>
<span class="line"><span>path: hosts.suis-macbook-air.photos        ← address within that context</span></span></code></pre></div><p>Read aloud: <em>entering through this host, running this app, reading the context <code>jabellae.cleaker.me</code>, at the path that points to photos physically held by the host <code>suis-macbook-air</code>.</em> The same <code>photos</code> app, pointed at a different namespace, resolves and renders completely different data — this is exactly <code>this.gui</code>&#39;s existing pattern (<code>BlocksTable</code>, <code>UsersTable</code>, <code>HostSurface</code>: one component, <code>endpoint</code>/<code>namespace</code> props decide what it shows), just now named precisely.</p><h2 id="_3-namespaces-hold-references-not-the-weight" tabindex="-1">3. Namespaces hold references, not the weight <a class="header-anchor" href="#_3-namespaces-hold-references-not-the-weight" aria-label="Permalink to &quot;3. Namespaces hold references, not the weight&quot;">​</a></h2><p>Heavy data stays where it was produced — a host&#39;s local filesystem, its own kernel, mesh storage, an encrypted blob, a phone, whatever physically holds it. The namespace never becomes a Dropbox. What a namespace holds is the <strong>resolvable shape</strong>: <code>photos[]</code>, <code>people[]</code>, <code>hosts.suis-macbook-air.photos → { __ptr: &quot;...&quot; }</code>. <code>{ __ptr }</code> is not a new mechanism — <code>BlocksTable.tsx</code>&#39;s <code>formatResolvedValue()</code> already renders it distinctly (a <code>→</code> arrow, <code>share</code> icon) — this doc just names the pattern it was already built for: a namespace path resolves to a pointer, and following that pointer is a separate hop, potentially against a different host entirely.</p><p>This is also the answer to §1&#39;s &quot;resolves to nothing&quot; claim: <code>&lt;host&gt;/@&lt;user&gt;</code> alone has nowhere to write, because a bare host carries no ledger. <code>&lt;host&gt;/@&lt;user&gt;.&lt;namespace&gt;</code> does — the write lands in the <em>namespace&#39;s</em> ledger, not the host&#39;s, and the host is recorded (if at all) only as one more path inside it (<code>hosts.&lt;host&gt;.*</code>), never as the ledger&#39;s root.</p><h2 id="_4-two-ledgers-were-being-shown-as-one" tabindex="-1">4. Two ledgers were being shown as one <a class="header-anchor" href="#_4-two-ledgers-were-being-shown-as-one" aria-label="Permalink to &quot;4. Two ledgers were being shown as one&quot;">​</a></h2><p>Everything a namespace holds has, until now, rendered as a single undifferentiated list in the Blockchain tab (<code>BlocksTable.tsx</code>). It is actually at least two kinds of entry, plus a third this session&#39;s work already produced:</p><table tabindex="0"><thead><tr><th>Ledger</th><th>What it records</th><th>Concrete entries</th></tr></thead><tbody><tr><td><strong>Claim ledger</strong> (cleaker)</td><td>Identity ↔ context bindings — <em>&quot;this <code>.me</code> reclaimed this namespace, with this proof, at this moment, from this surface.&quot;</em> Not content. The connective tissue <a href="./The-Model.html">The Model</a> calls <code>cleaker(me)</code>.</td><td><code>identityHash</code>, <code>publicKey</code>, <code>proof</code>, <code>openedAt</code>/<code>createdAt</code>, the <code>users.&lt;handle&gt;</code> pointer</td></tr><tr><td><strong>Content ledger</strong> (namespace)</td><td>The namespace&#39;s own addressable tree — unlocked once the claim ledger has an established binding for it.</td><td><code>profile.name</code>, <code>photos[]</code>, <code>hosts.&lt;host&gt;.photos</code>, <code>groups.*</code></td></tr><tr><td><strong>Surface ledger</strong> (host)</td><td>Self-reported telemetry about a <em>host</em>, not a user&#39;s content — see <a href="https://neurons-me.github.io/monad/Typescript/typedocs/KnowledgeGraph" target="_blank" rel="noreferrer">Knowledge Graph</a> §4.</td><td><code>surface.host.cpu</code>, <code>surface.host.memory</code>, <code>surface.usage.*</code></td></tr></tbody></table><p><code>.me</code> holds identity with no context at all. <code>cleaker</code> is the ledger that connects <code>.me</code> to a context — its job is recording <em>relations</em>, never storing &quot;the stuff.&quot; The namespace&#39;s own content ledger is what that relation unlocks. The host contributes storage, compute, and a surface — never a ledger of its own.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.me kernel  = who</span></span>
<span class="line"><span>cleaker     = who ↔ context   (the claim ledger)</span></span>
<span class="line"><span>namespace   = context content (the content ledger)</span></span>
<span class="line"><span>host        = where execution happens (the surface ledger, self-report only)</span></span></code></pre></div><p>A Blockchain-tab UI that means to represent this honestly should separate these three, not list them as one undifferentiated stream — <code>Claim Events</code> / <code>Content Events</code> / <code>System · Host Events</code>.</p><h2 id="_5-anchored-apps-vs-context-following-apps" tabindex="-1">5. Anchored apps vs. context-following apps <a class="header-anchor" href="#_5-anchored-apps-vs-context-following-apps" aria-label="Permalink to &quot;5. Anchored apps vs. context-following apps&quot;">​</a></h2><p>§2 established that a namespace can serve as the parameter for a generic app (<code>photos</code>, resolving differently per <code>@user.namespace</code>). That is not universal — some apps must never switch context, no matter what session happens to be active in the same browser. An infrastructure-management surface like <code>local.netget</code> administering real DNS/domain records is exactly this case: it must not start reflecting a <em>different</em> namespace&#39;s domains just because a different <code>.me</code> logged into the same tab. Two shapes, not one:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>App anchored:</span></span>
<span class="line"><span>  scope   = fixed at deployment/configuration — a real cleaker() claim of its own</span></span>
<span class="line"><span>  session = decides WHO may operate it (auth), never WHAT it operates on</span></span>
<span class="line"><span></span></span>
<span class="line"><span>App context-following:</span></span>
<span class="line"><span>  scope   = namespace passed explicitly, per call/render</span></span>
<span class="line"><span>  session = decides who is observing/operating within that explicit scope</span></span></code></pre></div><p><code>local.netget</code> is anchored — what fixes what it manages <em>should</em> be its own claimed identity (see <a href="./Surface-Identity-Claims.html">Surface-Identity-Claims.md</a>, a design not yet implemented — today &quot;anchored&quot; is only a configuration convention, not yet a cryptographic guarantee); a logged-in session only ever gates permission, never scope. <code>local.cleaker</code> is a hybrid: its root is anchored (this deployment&#39;s own claim ledger — the local blockchain, fixed), but <em>within</em> that fixed root, <code>@user</code> is a real parameter (which claimed identity you&#39;re currently viewing/acting as). <code>photos</code>, <code>BlocksTable</code>, <code>UsersTable</code> are pure context-following — every one of their target namespaces already arrives as an explicit prop (<code>endpoint</code>/<code>namespaceRootUrl</code>), never inferred from &quot;whatever session happens to be active&quot; — which is the same property that makes them safe to reuse generically in the first place.</p><p>The failure mode this rule prevents: wiring an anchored app&#39;s data source to &quot;the active session&#39;s namespace&quot; instead of to its own fixed claim. That silently turns a stable infrastructure surface into something that drifts with whoever is logged in — the anchored/ context-following distinction exists specifically to catch that class of mistake before it ships.</p><h2 id="_6-what-this-doc-is-not" tabindex="-1">6. What this doc is not <a class="header-anchor" href="#_6-what-this-doc-is-not" aria-label="Permalink to &quot;6. What this doc is not&quot;">​</a></h2><ul><li>Not a new grammar. <a href="./NRP-Namespaces.html">NRP-Namespaces.md</a>&#39;s parser already accepts <code>jabellae.suis-macbook-air.local</code> as valid <em>syntax</em> — that doesn&#39;t change. This doc says which forms are the <em>semantically correct</em> target for a real write, not which forms parse.</li><li>Not a claim that hosts are unimportant. A host is where computation and storage actually happen — see §3. It simply isn&#39;t where a ledger&#39;s identity lives.</li><li>Not (yet) a description of a cross-host published ledger. A namespace already implies &quot;many hosts can write against the same context&quot; — that is not a separate future feature layered on top, it is what §2&#39;s designation already means once more than one host serves the same namespace.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./The-Model.html">The Model</a> — <code>cleaker()</code> fixes context, <code>cleaker(me)</code> mounts identity into it; this doc sharpens what &quot;context&quot; is allowed to be.</li><li><a href="./Surface-Identity-Claims.html">Surface-Identity-Claims.md</a> — design doc for making a surface&#39;s own claim (§5&#39;s &quot;anchored&quot; scope) cryptographically real, not just a configuration convention.</li><li><a href="./NRP-Namespaces.html">NRP-Namespaces.md</a> — the grammar this doc&#39;s terms parse under.</li><li><a href="https://neurons-me.github.io/monad/Typescript/typedocs/KnowledgeGraph" target="_blank" rel="noreferrer">Knowledge Graph</a> — the surface ledger (§4 above) worked out in full, including why it&#39;s unsigned self-report.</li><li><code>modules/monad/Typescript/src/namespace/identity.ts</code>, <code>modules/monad/Typescript/src/http/namespace.ts</code>&#39;s <code>resolveNamespace()</code> — where &quot;namespace&quot; is derived from a host today; the first code this doc&#39;s rule should be checked against.</li></ul>`,31)])])}const u=t(n,[["render",c]]);export{m as __pageData,u as default};
