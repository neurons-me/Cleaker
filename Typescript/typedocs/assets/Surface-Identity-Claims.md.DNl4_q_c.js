import{_ as t,o as a,c as s,a0 as o}from"./chunks/framework.xN4chThJ.js";const m=JSON.parse('{"title":"Surface Identity Claims","description":"","frontmatter":{},"headers":[],"relativePath":"Surface-Identity-Claims.md","filePath":"Surface-Identity-Claims.md"}'),i={name:"Surface-Identity-Claims.md"};function n(r,e,c,d,l,h){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="surface-identity-claims" tabindex="-1">Surface Identity Claims <a class="header-anchor" href="#surface-identity-claims" aria-label="Permalink to &quot;Surface Identity Claims&quot;">​</a></h1><p><strong>neurons.me / suiGn</strong><strong>Status: DESIGN.</strong> Nothing in this document is implemented. It records a target shape and the current gap against it, verified against real code on 2026-08-28 — not a shipped feature. Treat every claim below about <em>current</em> code as fact-checked; every claim about the target state as a proposal. <strong>License:</strong> CC0 1.0 Universal — Public Domain</p><hr><h2 id="_0-the-one-sentence-version" tabindex="-1">0. The one-sentence version <a class="header-anchor" href="#_0-the-one-sentence-version" aria-label="Permalink to &quot;0. The one-sentence version&quot;">​</a></h2><p>A namespace&#39;s claim ledger already lets a human <code>.me</code> prove &quot;I am who I say I am, here&quot; — this doc proposes the same primitive for a <em>surface</em> (a monad, a netget instance, any running service), so &quot;this server is who it says it is&quot; stops being a bare string in a JSON registry and becomes the same kind of verifiable claim.</p><h2 id="_1-where-this-actually-starts-verified-not-assumed" tabindex="-1">1. Where this actually starts (verified, not assumed) <a class="header-anchor" href="#_1-where-this-actually-starts-verified-not-assumed" aria-label="Permalink to &quot;1. Where this actually starts (verified, not assumed)&quot;">​</a></h2><p>It would be easy to assume surfaces need a stronger identity built from scratch. They don&#39;t — the identity side is already solid; only the claim is missing.</p><p><code>selfMapping.ts</code>&#39;s <code>ensureCleakerIdentityConfig()</code> already:</p><ul><li>Generates a real Ed25519 keypair (<code>crypto.generateKeyPairSync(&quot;ed25519&quot;)</code>) for every surface that doesn&#39;t already have one.</li><li>Persists it to <code>self.keys.json</code> (<code>chmod 0600</code>) so it survives restarts.</li><li>Explicitly refuses to derive it from <code>SEED</code>/<code>NAMESPACE_SEED</code> — the code comment states the reason directly: that would make two monads sharing a namespace seed collide as the same surface identity, conflating &quot;authority over the semantic namespace&quot; with &quot;identity of this runtime instance.&quot; Those are different things and the code already keeps them apart.</li><li>The old SEED-derivation function (<code>deriveEd25519KeyPairFromSeed</code>, HKDF-SHA256 over the seed) is still in the file but has zero callers anywhere in <code>src/</code> — dead code, safe to delete separately from this work.</li></ul><p>So: <strong>every surface already has a strong, persistent, non-collidable identity keypair.</strong> That was previously documented in <code>CLAUDE.md</code> as an open gap (&quot;surface key derivation bug&quot;) — it isn&#39;t, as of this doc. The actual gap is narrower and sits one layer up.</p><h2 id="_2-the-actual-gap-precisely" tabindex="-1">2. The actual gap, precisely <a class="header-anchor" href="#_2-the-actual-gap-precisely" aria-label="Permalink to &quot;2. The actual gap, precisely&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface keypair   — EXISTS   (§1)</span></span>
<span class="line"><span>surface claim     — MISSING  (§3)</span></span>
<span class="line"><span>proxy enforcement — MISSING  (§4)</span></span></code></pre></div><p>A surface&#39;s keypair proves nothing about which namespace it&#39;s entitled to serve. Nothing today binds <code>MONAD_PUBLIC_KEY</code>/<code>identityHash</code> to a specific namespace the way <code>claimNamespace()</code> binds a human&#39;s key to <code>jabellae.cleaker.me</code>. A surface can <em>say</em> <code>metadata.namespace: &quot;cleaker.me&quot;</code> in its own self-report and nothing checks whether it has any right to.</p><h2 id="_3-what-a-surface-claim-would-look-like" tabindex="-1">3. What a surface claim would look like <a class="header-anchor" href="#_3-what-a-surface-claim-would-look-like" aria-label="Permalink to &quot;3. What a surface claim would look like&quot;">​</a></h2><p>Reuse the exact mechanism that already exists for humans — <code>claimNamespace()</code> in <code>modules/monad/Typescript/src/claim/records.ts</code> — rather than inventing a parallel one. A surface claims a namespace the same way a person does: secret + proof, verified, persisted, hash-chained. The only thing that changes is <em>who</em> is doing the claiming.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface:   netget.site</span></span>
<span class="line"><span>identity:  &lt;surface&#39;s own persistent identityHash, from §1&#39;s keypair&gt;</span></span>
<span class="line"><span>claim:     netget.site  ↔  &lt;surface&#39;s own identity&gt;        (surface → its own namespace)</span></span></code></pre></div><p><strong>Note, added 2026-09-24 (see §7.3):</strong> the line above was originally written as <code>netget.site ↔ cleaker.me</code>, implying a surface could self-claim a namespace <em>other</em> than its own. That&#39;s wrong and now corrected here directly, not just annotated — this mechanism (self-claim via <code>claimNamespace()</code>) only ever covers a surface&#39;s <strong>own</strong> designated namespace. A surface serving a <em>different</em>, already-claimed namespace (the <code>netget.site</code> ↔ <code>cleaker.me</code> case this example used to show) is never a self-claim — it requires the <code>cleaker.me</code> claim holder to have delegated that right, per §7.3. Do not read this section as licensing a surface to claim a namespace it doesn&#39;t already own.</p><p>This is the same shape as <a href="./Namespace-Is-Context.html">Namespace-Is-Context.md</a> §4&#39;s claim ledger — &quot;this <code>.me</code> reclaimed this namespace, with this proof, at this moment, from this surface&quot; — except the <code>.me</code> doing the reclaiming belongs to infrastructure, not a person. Hosts, apps, surfaces, gateways: anything with a persistent keypair can <code>cleaker()</code> itself into a namespace. Humans are not a special case of this mechanism — they were just the first user of it.</p><p>Once claimed, this is what makes §5 of Namespace-Is-Context.md&#39;s &quot;anchored&quot; scope <em>cryptographically</em> real instead of a configuration convention someone could misconfigure or spoof — <code>local.netget</code>&#39;s claim to its own namespace becomes checkable the same way a user&#39;s claim is, not just declared in a JSON file nobody verifies.</p><h2 id="_4-why-trust-tiers-are-not-this" tabindex="-1">4. Why trust tiers are not this <a class="header-anchor" href="#_4-why-trust-tiers-are-not-this" aria-label="Permalink to &quot;4. Why trust tiers are not this&quot;">​</a></h2><p><code>surface_proxy.lua</code> already has machinery that looks adjacent to this and isn&#39;t: a trust-tier system (<code>owner &gt; admin &gt; peer &gt; guest</code>, computed at registration time by <code>apps.lua</code>&#39;s <code>derive_trust()</code> against <code>gateway-claims.json</code>), plus a <code>WARNING</code> log when two live candidates at the same trust tier disagree on <code>identityHash</code> for the same host. That&#39;s relative ranking among whoever registered, not cryptographic verification of anyone&#39;s right to the namespace. The question it answers is <em>&quot;which of these registered candidates should I currently prefer?&quot;</em> The question a claim answers is different and stricter: <em>&quot;does this identityHash hold an actual, currently-valid claim for this namespace?&quot;</em> Trust tiers can keep existing as a tiebreaker among multiple surfaces that all hold <strong>valid</strong> claims to the same namespace (legitimate replicas) — they were never meant to substitute for checking the claim exists at all, and today nothing does that check.</p><h2 id="_5-the-target-test" tabindex="-1">5. The target test <a class="header-anchor" href="#_5-the-target-test" aria-label="Permalink to &quot;5. The target test&quot;">​</a></h2><p>The one that actually proves this closed — mirrors the adversarial, live-verified standard this session&#39;s earlier claim-proof closure used (real <code>curl</code> against a running monad, not a unit test in isolation):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>same namespace string</span></span>
<span class="line"><span>same trust tier</span></span>
<span class="line"><span>different identityHash</span></span>
<span class="line"><span></span></span>
<span class="line"><span>today:   ngx.WARN, routing proceeds to whichever wins the trust/recency tiebreak</span></span>
<span class="line"><span>target:  reject — the candidate without a valid claim for this namespace never becomes \`best\`</span></span></code></pre></div><p>This is the test that actually distinguishes &quot;we rank registrations&quot; from &quot;we verify identity&quot; — today&#39;s code passes the first framing and fails the second. A future implementation is only done when this specific scenario, run live, rejects instead of warns.</p><h2 id="_6-phases-not-started" tabindex="-1">6. Phases (not started) <a class="header-anchor" href="#_6-phases-not-started" aria-label="Permalink to &quot;6. Phases (not started)&quot;">​</a></h2><ol><li><code>namespace-is-context.md</code> (done) — anchored vs. context-following named as a rule.</li><li>This doc (done) — the gap named precisely: keypair exists, claim missing, enforcement missing.</li><li>Implement: a surface calls <code>claimNamespace()</code> for its own designated namespace using its already-persistent keypair (§1) as the claim identity, at bootstrap.</li><li>Implement: <code>surface_proxy.lua</code> (or the Node-side registration check feeding it) verifies a candidate&#39;s <code>identityHash</code> against an actual claim record for the namespace being served, before it&#39;s eligible to become <code>best</code> — trust tier remains a tiebreaker among valid claims only.</li><li>The §5 spoof test, run live against a real gateway, both before and after step 4, to demonstrate the actual behavior change (warn → reject).</li></ol><p>Nothing past step 2 is built <strong>in the specific shape this document proposes</strong> (a surface self-claiming its own namespace via its own persistent keypair, §3, then <code>surface_proxy.lua</code> enforcing that at routing time, §4). Do not read §3&#39;s claim shape as implemented.</p><p>A related but distinct piece of progress happened 2026-09-12, worth naming precisely so it isn&#39;t confused with §3-5 above: netget&#39;s gateway-setup claim flow (<code>gatewaySetupSession.ts</code>&#39;s <code>commitSignedClaim</code>) now verifies a real, currently-active keychain key + Ed25519 signature against the namespace an <em>operator</em> is signed into, before trusting them as that gateway&#39;s administrator — real cryptographic verification, not just trust-tier ranking. But this is netget consulting an external operator&#39;s claim, not a surface claiming its own namespace as this doc&#39;s §3 describes, and it does nothing for §4&#39;s routing-time enforcement gap (still open, still gated the same way — see <code>CLAUDE.md</code>&#39;s &quot;Known architectural gaps&quot; #2, updated 2026-09-12 to say exactly this). This document&#39;s own Phases table is otherwise unchanged.</p><h2 id="_7-addendum-—-2026-09-24-netget-is-a-pointer-not-an-installation" tabindex="-1">7. Addendum — 2026-09-24: netget is a pointer, not an installation <a class="header-anchor" href="#_7-addendum-—-2026-09-24-netget-is-a-pointer-not-an-installation" aria-label="Permalink to &quot;7. Addendum — 2026-09-24: netget is a pointer, not an installation&quot;">​</a></h2><p><strong>Status: DESIGN.</strong> Nothing below is implemented, except where explicitly marked verified. This addendum grew out of a long design conversation working forward from §1-6 above, and corrects one real overreach that conversation initially made (§9) before arriving at the shape below.</p><h3 id="_7-1-the-engine-is-liminal-the-config-is-per-namespace" tabindex="-1">7.1 The engine is liminal; the config is per-namespace <a class="header-anchor" href="#_7-1-the-engine-is-liminal-the-config-is-per-namespace" aria-label="Permalink to &quot;7.1 The engine is liminal; the config is per-namespace&quot;">​</a></h3><p>Two things kept getting conflated under the one word &quot;netget&quot;: the <strong>engine</strong> — the OpenResty/Lua process that physically holds sockets, certs, and the nginx worker, and does subtractive-synthesis routing across every namespace currently anchored to this host (<code>monadIndex.ts</code>, <code>meshSelect.ts</code>) — and the <strong>declared configuration</strong> a namespace wants that engine to act on (which domain, which port, which cert). The engine is genuinely cross-namespace by necessity — it has to see all anchored trees to route at all, the same reason a host-owner view (<code>monads</code> CLI, subtractive synthesis) is legitimately different from a single participant&#39;s view. The configuration is not — it is ordinary data belonging to one namespace, exactly like any other branch that namespace already owns.</p><p><strong>Target shape:</strong> <code>&lt;namespace&gt;.netget.*</code> (domains, ports, cert requests, the app registry) is plain tree data under that namespace&#39;s own root, governed by the <em>same</em> write-authorization every other write to that namespace already requires — <code>isNamespaceWriteAuthorized()</code> against <code>getClaim(namespace)</code> (<code>replay.ts</code> / <code>records.ts</code>), nothing new. The engine reads across every namespace&#39;s <code>.netget</code> branch the same way it already reads across every entry in the mesh index — it was never &quot;inside&quot; one namespace&#39;s tree to begin with, so nothing about scoping configuration this way changes what the engine can see.</p><h3 id="_7-2-doors-are-pointers-not-separate-installations" tabindex="-1">7.2 Doors are pointers, not separate installations <a class="header-anchor" href="#_7-2-doors-are-pointers-not-separate-installations" aria-label="Permalink to &quot;7.2 Doors are pointers, not separate installations&quot;">​</a></h3><p><code>netget.site</code>, <code>cleaker.me</code>, <code>local.cleaker</code>, <code>local.host</code> are not four different kinds of thing — each is a namespace, and which namespace currently <em>answers</em> as a given door is a <code>__ptr</code> resolution, the same operator already documented as <code>.me</code> operator #1 (<code>isPointerCall</code>, <code>Typescript/typedocs/Operators.md</code>) and already proven live elsewhere in this exact codebase: <code>records.ts</code>&#39;s <code>materializeProjectedNamespaceClaim()</code> already writes <code>{ __ptr: namespace }</code> at <code>daemon.users.&lt;host&gt;.&lt;username&gt;</code> to resolve a handle to its full namespace. A door pointer is the identical shape, one level up: <code>netget.main.server -&gt; &lt;namespace currently serving as the main door&gt;</code>. Reading <code>netget.site.domains</code> and reading through the pointer both land on the same node — not two systems that happen to agree, one node reached two ways. Repointing a door is one signed <code>__ptr</code> write by whoever holds the pointer&#39;s containing branch, never a data migration and never a manual nginx edit.</p><p><strong>Verified, not proposed:</strong> this is not a new idea invented in this addendum. Branch <code>migration/main-server-doors</code> (<code>modules/netget/Typescript</code>, commit <code>a7595e3</code>, 2026-09-20 — committed locally, not yet pushed/deployed) already implements exactly this: domain records carry a <code>namespaceId</code>, <code>namespaceIdentity.ts</code> and <code>lua/lib/main_server.lua</code> resolve a door by identity rather than by hostname, and an older door registered without an identity is pointed at one — &quot;no hostname defines the namespace; the state names it by <code>{id, label}</code>.&quot; This addendum&#39;s §7.2 is a restatement of already-committed work, not a new proposal — it&#39;s included here because it&#39;s the piece that makes §7.1 and §3-4 consistent with each other (see §7.3).</p><h3 id="_7-3-why-this-replaces-the-free-floating-gatewayid" tabindex="-1">7.3 Why this replaces the free-floating <code>gatewayId</code> <a class="header-anchor" href="#_7-3-why-this-replaces-the-free-floating-gatewayid" aria-label="Permalink to &quot;7.3 Why this replaces the free-floating \`gatewayId\`&quot;">​</a></h3><p>§3-4 above and this session&#39;s earlier design pass both reached for a <em>separate</em> ledger keyed independently of any namespace — <code>gatewayAuthority.ts</code>&#39;s <code>GatewayAuthorityRecord</code>, keyed by a free-standing <code>gatewayId</code> string, and (in-conversation only, never written to this doc) a proposed <code>ServeDelegationRecord</code> with the same shape. Both repeat the same mistake: a <code>gatewayId</code> (or a delegation record) that is <em>related to</em> a namespace via a guard function (<code>isNamespaceLocalToThisInstallation</code>) instead of <em>being</em> that namespace&#39;s own branch invites the two drifting apart — two ids resolving to one namespace, or a guard that must never be wrong instead of a structural impossibility.</p><p>Once doors are pointers (§7.2) and configuration is namespace-scoped (§7.1), the reason <code>gatewayAuthority.ts</code> gives for its separate storage — &quot;a gateway&#39;s authority belongs to the INSTALLATION... transferring ownership must never require moving state into a different user&#39;s personal branch&quot; — still deserves an answer, but not a parallel ledger: a namespace&#39;s claim is today single-owner/all-or-nothing (<code>records.ts</code> has no multi-admin or transfer concept), and that gap is real. The fix is a <code>&lt;namespace&gt;.netget.delegates</code> branch — same vigencia/autorización split <code>gatewayAuthority.ts</code> already proved out (<code>getKeychainKey(...).authorization === &quot;active&quot;</code> + membership check against the branch&#39;s own state, signed via <code>isNamespaceWriteAuthorized</code>, nonce anti-replay) — but keyed by the namespace itself, not by an independent id. Multi-admin and delegation become a capability every namespace has, not a netget-specific side-ledger.</p><h3 id="_7-4-the-concrete-small-fix-—-still-first-in-line" tabindex="-1">7.4 The concrete, small fix — still first in line <a class="header-anchor" href="#_7-4-the-concrete-small-fix-—-still-first-in-line" aria-label="Permalink to &quot;7.4 The concrete, small fix — still first in line&quot;">​</a></h3><p>Independent of §7.1-7.3, <code>meshAnnounce.ts:166</code>&#39;s <code>isNamespaceUsableByIdentity()</code> compares the announced <code>identityHash</code> — documented in three places in this codebase as a public, non-secret fingerprint — against <code>claim.identityHash</code>, when what the signature actually proves possession of is <code>public_key</code>. <code>ClaimRecord.publicKey</code> already exists (<code>records.ts:249</code>) and is never cross-checked here. Binding the check to <code>claim.publicKey === entry.public_key</code> (ownership case) or membership in <code>&lt;namespace&gt;.netget.delegates</code> (delegated-serve case, §7.3) closes a real, presently-exploitable gap in shipped code with no new storage and no new crypto primitive — this remains the first implementation step, unchanged from the order agreed earlier in this conversation: (1) this fix, (2) the <code>.netget.delegates</code> write shape, (3) the same check ported to the local <code>/apps/report</code> path and to a WS edge node&#39;s first connect, (4) the <code>/netget</code> window gated by resolving the main-server pointer (§7.2) to a namespace and checking that namespace&#39;s own claim/delegates, (5) WS tunnel multiplexing at the mothership, last, on top of an already-verified claim — never before it.</p><h3 id="_7-5-closing-framing" tabindex="-1">7.5 Closing framing <a class="header-anchor" href="#_7-5-closing-framing" aria-label="Permalink to &quot;7.5 Closing framing&quot;">​</a></h3><p>Under this shape, <code>.me</code> is closer to a semantic programming language than a database: a path is resolved, not looked up, and everything — a door, <code>netget</code>, an endpoint, a device — is a name that means only what it currently points to or claims. <code>netget</code> is not a privileged keyword; it is one more name that happens, today, to point at a namespace running an OpenResty/Lua engine, the same way <code>-&gt;</code> lets any other name point at any other tree. Naming a path <em>is</em> the only interface to whatever structure materializes it — a monad process, cleaker&#39;s namespace resolution, an OpenResty worker — nothing about the language changes to reach any of them; only what the name currently resolves to does.</p><h3 id="_7-6-risks-carried-forward-not-yet-resolved" tabindex="-1">7.6 Risks carried forward, not yet resolved <a class="header-anchor" href="#_7-6-risks-carried-forward-not-yet-resolved" aria-label="Permalink to &quot;7.6 Risks carried forward, not yet resolved&quot;">​</a></h3><p>Three things the shape above does not itself solve — named so they aren&#39;t lost between this addendum and whoever implements it:</p><p><strong>Two ledgers at once.</strong> For as long as <code>daemon.gateways.&lt;gatewayId&gt;</code> (<code>gatewayAuthority.ts</code>) and <code>&lt;namespace&gt;.netget.delegates</code> (§7.3) both exist, something will keep writing to the old one out of habit or unfamiliarity with this addendum. §7.3 argues the old ledger should stop being used for new installs; it does not yet say what happens to an <em>already-bootstrapped</em> <code>gatewayId</code> record, and that migration/deprecation path — not indefinite coexistence — needs its own design pass before implementation starts, not after.</p><p><strong>Liminal is not the same as public.</strong> The engine (§7.1) can see every namespace&#39;s <code>.netget</code> branch because it has to, to route at all — that is a <em>capability</em>, not a <em>license</em>. Reading <code>&lt;namespace&gt;.netget.*</code> through the <code>/netget</code> window, or relaying it to a WS mothership client (§4/§7.4&#39;s future work), still has to go through the same disclosure contract every other read of that namespace does (<code>pathResolver.ts</code>&#39;s public/closed envelope) — the engine&#39;s structural position outside every namespace&#39;s tree does not exempt it from that namespace&#39;s own privacy rules.</p><p><strong>Unclaimed namespaces need a harder rule off loopback.</strong> <code>isNamespaceUsableByIdentity()</code>&#39;s <code>if (!claim) return true</code> (§7.4) is fine on the local, loopback-only path — physical access to the machine already implies a trust boundary. On an open remote/WS registration endpoint it is not: without a stated policy, &quot;whoever announces an unclaimed namespace first, wins&quot; becomes exactly the race a signed first-claim (§3, tightened by the note above) exists to prevent. §7.4&#39;s implementation order already assumes this gets resolved before step 3 (WS first-connect) ships; this paragraph makes that assumption an explicit, named requirement rather than an implicit one.</p><h3 id="_7-7-netget-delegates-shape-and-the-write-path-gaps-it-depends-on-closing-first" tabindex="-1">7.7 <code>.netget.delegates</code>: shape, and the write-path gaps it depends on closing first <a class="header-anchor" href="#_7-7-netget-delegates-shape-and-the-write-path-gaps-it-depends-on-closing-first" aria-label="Permalink to &quot;7.7 \`.netget.delegates\`: shape, and the write-path gaps it depends on closing first&quot;">​</a></h3><p><strong>Status: prerequisites CLOSED (2026-09-24, monad commits <code>71126411</code>, <code>909f170e</code>, <code>6ab01be0</code>; GUI commit <code>7522bea8</code>) — see §7.8 for what the closure work actually found, including one gap that remains open. The <code>.netget.delegates</code> shape below is still not implemented; only the write-path gaps it depends on are closed.</strong> Everything in this section was checked directly against <code>handlers/commandHandler.ts</code>&#39;s <code>rootCommandHandler</code> (the generic namespace-write surface every plain write, including a future <code>.netget.delegates</code> write, goes through). Three real gaps were found there, not hypothesized — without closing them first, this shape would be actively unsafe, not just incomplete.</p><p><strong>Purpose</strong>, unchanged from the shape sketched earlier in this conversation: <code>&lt;namespace&gt;.netget. delegates</code> answers &quot;who besides the namespace&#39;s own claim holder may serve this namespace&#39;s traffic, or write its <code>.netget.*</code> config, without holding the claim&#39;s own private key?&quot; — read by <code>meshAnnounce.ts</code>&#39;s <code>isNamespaceUsableByIdentity()</code> fix (§7.4) and by any future delegate-authored write to <code>&lt;namespace&gt;.netget.*</code>.</p><p><strong>Prerequisite 1 — the unclaimed-namespace bypass is real, not theoretical.</strong><code>rootCommandHandler</code> (<code>commandHandler.ts:226</code>) reads <code>const claim = getClaim(namespace)</code>, then gates the signature check behind <code>if (claim) { ... }</code> — for a namespace with no claim, that whole block is skipped and the write proceeds using <code>body.identityHash</code> taken <strong>unverified, straight from the caller</strong> (<code>commandHandler.ts:239-241</code>). Nothing stops a write to <code>netget.delegates</code> for an unclaimed namespace today, and <code>meshAnnounce.ts</code> would read it as a real delegation. The fix has an exact precedent two guards above it in the same function: <code>isKeychainReservedPath()</code> and <code>isGatewayAuthorityReservedPath()</code> (<code>commandHandler.ts:204-216</code>) already reserve certain paths so they 403 unconditionally, before the <code>if (claim)</code> gate, rather than falling through it. <code>netget. delegates</code> (arguably all of <code>&lt;namespace&gt;.netget.*</code>) needs the identical reserved-path treatment — copy the pattern, don&#39;t invent a new one.</p><p><strong>Prerequisite 2 — <code>isNamespaceWriteAuthorized()</code> has no replay or namespace binding.</strong> Verified directly in <code>replay.ts</code>: the function signs <code>toStableJson(stripWriteAuthFields(body))</code> — no nonce, no timestamp, and <code>namespace</code> is never part of the signed payload (it comes from <code>resolveNamespace(req)</code>, outside the signature entirely). Two consequences, both real: (a) a previously-valid signed grant can be replayed at any later time — including <em>after</em> a revoke, since §7&#39;s original design deleted the delegate entry on revoke but never made the grant&#39;s own signature single-use, so replaying the old grant body silently restores a revoked delegate; &quot;a repeated grant is harmless&quot; (this doc&#39;s own earlier framing) is wrong for exactly this reason, corrected here. (b) if one key holds claims on two different namespaces, a signed write meant for one could be replayed against the other, since nothing in the signature ties it to a specific namespace.</p><p>Fix: bind the signed payload to <code>{ ...fields, namespace, expectedHeadHash }</code>, where <code>expectedHeadHash</code> is the signing moment&#39;s own read of that namespace&#39;s current chain head (the latest entry&#39;s <code>.hash</code> from <code>getMemoriesForNamespace(namespace)</code>, already real, already hash-chained per axiom A8). The server rejects the write if the chain has moved since. This makes every signature valid for exactly one write, in one namespace, at one moment — no nonce cache to maintain. Tradeoff worth stating plainly: a legitimate concurrent writer racing another write to the same namespace gets a stale-head rejection too and must re-sign against the new head — acceptable given a claim has exactly one owner, so genuine concurrent writers are rare by construction, not absent.</p><p><strong>Prerequisite 3 — delegates must not be able to grant delegates.</strong> The second use case (§7&#39;s own purpose) requires <code>isNamespaceWriteAuthorized</code>&#39;s caller to eventually accept a <em>delegate&#39;s</em> key, not just the claim holder&#39;s, for writes under <code>&lt;namespace&gt;.netget.*</code>. <code>netget.delegates</code> is itself under that prefix — without an explicit carve-out, a delegate holding any write-shaped scope could grant itself (or anyone) more delegates, escalating past whatever scope it was actually given. <code>netget.delegates</code> must stay owner-key-only, unconditionally, enforced as an allowlist (&quot;only these exact paths accept a delegate signature at all; everything else denied&quot;) — never a blocklist that only excludes what someone thought to name.</p><p><strong>The corrected shape</strong>, incorporating all three fixes plus two smaller corrections:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NetgetDelegateEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  publicKey</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SPKI DER→PEM, same conversion convention as records.ts&#39;s</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                        // rawEd25519PublicKeyToPem() (prefix 302a300506032b6570032100) —</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                        // reused for consistency, not reinvented.</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  scopes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// opaque, same convention as gatewayAuthority.ts&#39;s grants.</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  grantedAt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  grantedBy</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// identityHash of the claim holder at grant time — informational audit</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                         // trail only; authority is always re-derived from getClaim() at check</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                         // time, never cached from this field.</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// human-readable, informational.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Map key: sha256 hex of the SPKI DER bytes (not the PEM text — whitespace/newline variance</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// would make the same key hash differently). Server recomputes this from \`publicKey\` on every</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// write (rejecting a mismatched map key) and on every read (never trusts the stored key blindly).</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NetgetDelegates</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NetgetDelegateEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><p>Dropped the earlier <code>namespace</code> field from the record entirely — it only ever repeated what the kernel path already encodes unambiguously, and a namespace that disagreed with its own storage location would just be a bug with no principled way to pick a winner.</p><p><strong>Left open, not decided here:</strong> what <code>me://&lt;namespace&gt;/netget/delegates</code> discloses to an unauthenticated reader through the same public/closed envelope <code>pathResolver.ts</code> already implements (three classifications — <code>public</code>, <code>closed</code>, <code>not_found</code>; <code>closed</code> deliberately indistinguishable from stealth on the wire). Public keys reveal nothing sensitive; a <code>label</code> like <code>&quot;mothership-1&quot;</code> can reveal real infrastructure topology. Needs its own decision before this ships, not an assumption baked into the shape.</p><p><strong>Closes the loop on §7.6&#39;s earlier verifiability discussion — corrected, not as first written.</strong> Verified directly in <code>memoryStore.ts</code>: the <code>.me</code> kernel&#39;s <code>hash</code>/<code>prevHash</code> fields are a single <strong>global</strong> chain (<code>getKernel().memories</code>, one sequence for the whole kernel, axiom A8&#39;s own &quot;hash-chain integrity across all memories&quot; phrasing was literal) — not one chain per namespace. <code>getNamespaceChainHead()</code> (§7.8) constructs its own namespace-<em>projected</em> view (the last entry whose path resolves under that namespace, filtered from the global list) to get a value that changes exactly when that namespace&#39;s own data changes — which is the real property replay protection needs — but the entry actually written by that signed request does <strong>not</strong> carry that projected value as its own literal <code>prevHash</code> (its real <code>prevHash</code> links to whatever else was globally most recent, possibly a different namespace&#39;s write). So: the anti-replay mechanism is correct and live-verified (§7.8&#39;s tests), but &quot;this signed head is a state commitment, provable by replaying the namespace&#39;s own chain from genesis&quot; overstated it — there is no literal namespace- scoped chain to replay; there is a projection this function recomputes fresh each time. A real per-namespace, replayable state commitment is a design this doc doesn&#39;t claim to have built.</p><h3 id="_7-8-closing-the-two-prerequisites-—-what-the-actual-implementation-found" tabindex="-1">7.8 Closing the two prerequisites — what the actual implementation found <a class="header-anchor" href="#_7-8-closing-the-two-prerequisites-—-what-the-actual-implementation-found" aria-label="Permalink to &quot;7.8 Closing the two prerequisites — what the actual implementation found&quot;">​</a></h3><p><strong>Status: CLOSED for the scope below; one gap explicitly left open, not silently accepted.</strong> monad commits <code>71126411</code> (prerequisite 1, netget.* reserved-path guard), <code>909f170e</code> (prerequisite 2, chain-head binding on <code>rootCommandHandler</code>), <code>6ab01be0</code> (both, ported to <code>commitHandler</code> after review found it was the only write path checked); GUI commit <code>7522bea8</code> (the one real production signer, <code>createCleakerSession.ts</code>&#39;s <code>signAndWrite</code>, updated to match).</p><p><strong>Every other write entry point was audited, not assumed safe.</strong> <code>appendSemanticMemory()</code>&#39;s own callers were grepped exhaustively: <code>session.ts</code>, <code>claims.ts</code>, <code>usageLedger.ts</code>, <code>hostTelemetryLedger.ts</code>, <code>Blockchain/users.ts</code>, <code>keychain.ts</code>, <code>records.ts</code>, <code>claimSemantics.ts</code>, <code>semanticBootstrap.ts</code> all write to hardcoded or internally-derived paths, never an attacker-chosen one — none share the exposure. <code>commitHandler</code> (<code>POST /api/v1/commit</code>) was the one real exception: it already had the keychain/gateway-authority/routing-record guards, but neither the netget.* guard nor chain-head binding, and was fixed to match.</p><p><strong>A real, live-reproducible bug was found while testing this, not a test-harness quirk.</strong><code>getNamespaceChainHead()</code>&#39;s first version was contaminated by <code>hostTelemetryLedger.ts</code>/ <code>usageLedger.ts</code>, which write <code>surface.host.*</code>/<code>surface.usage.*</code> into the monad&#39;s own namespace on an interval/per-request basis. When a claimed namespace IS the monad&#39;s own self identity — the exact <code>netget.site</code> self-claim case §7.3 describes as the target shape — this telemetry noise moved that namespace&#39;s chain head out from under a legitimate signed write between the client&#39;s read and its write landing, producing a spurious <code>STALE_HEAD</code> reliably, not rarely. Fixed by excluding the <code>surface.</code> prefix from the head computation.</p><p><strong>Left open, deliberately, not silently:</strong> <code>expectedHeadHash</code> binds to the <em>caller&#39;s own</em> claimed namespace. A commit whose event targets a <em>different</em> namespace — <code>commitHandler</code>&#39;s own shared-group-root case, where a caller writes into a namespace nobody individually owns — never moves the caller&#39;s own head, so replaying such a commit is not yet rejected. A test (<code>commitGate.test.ts</code>, &quot;KNOWN GAP&quot;) asserts today&#39;s actual behavior and says explicitly to invert the assertion once this is closed, rather than passing silently. Closing it needs per-event or joint-namespace-set head binding — a harder design than this pass attempted.</p><p><strong>Two decisions surfaced, not made unilaterally:</strong></p><ul><li>The single-process assumption: <code>rootCommandHandler</code>&#39;s check-then-write has no <code>await</code> between reading the head and writing, so two concurrent requests can&#39;t interleave — but this is a single-Node-process guarantee only. If the monad ever runs as a cluster or multiple instances over shared storage, two writes against the same head could both land. Not resolved here; recorded as an explicit assumption. The alternative, if it&#39;s ever needed, is a storage-level unique constraint on <code>(namespace, prevHash)</code> rather than an application-level check.</li><li><code>GET /api/v1/write-head</code> is unauthenticated and public: anyone can observe <em>that</em> a namespace&#39;s state just changed (a changing hash), never <em>what</em> changed. Left as-is for now — narrower than the existing public <code>namespace-owner</code> endpoint already discloses — but worth naming as a choice rather than an oversight.</li></ul><h3 id="_7-9-two-more-real-gaps-a-review-surfaced-both-closed-two-host-visibility-precisions" tabindex="-1">7.9 Two more real gaps a review surfaced, both closed; two host-visibility precisions <a class="header-anchor" href="#_7-9-two-more-real-gaps-a-review-surfaced-both-closed-two-host-visibility-precisions" aria-label="Permalink to &quot;7.9 Two more real gaps a review surfaced, both closed; two host-visibility precisions&quot;">​</a></h3><p><strong>Status: CLOSED (monad commit <code>70d964f7</code>).</strong></p><p><strong>The <code>surface.*</code> exclusion (§7.8) created a new replay hole.</strong> Anything the head computation excludes also never moves the head an external caller&#39;s signature could be checked against — so a signed write to <code>surface.*</code> would have stayed valid to replay indefinitely, the same class of gap as the multi-namespace one, except this one was introduced by the fix itself rather than pre- existing. Closed by reserving <code>surface.*</code> to internal callers only (<code>isSurfaceTelemetryReservedPath</code></p><ul><li>the existing internal-token check, same pattern as <code>isGatewayRoutingRecordPath</code>) in both write paths. <strong>General rule, worth keeping:</strong> any prefix excluded from head computation must also be write-restricted to internal callers — the exclusion alone is never sufficient by itself.</li></ul><p><strong>A genuinely more severe, previously-unexamined vulnerability</strong>, found by asking what a root- claim-signed write with <code>path: &quot;users.alice.profile.email&quot;</code> actually does. The monad&#39;s own root namespace resolves to an <em>empty</em> kernel prefix (<code>namespaceToKernelPrefix</code>), so such a write is never further prefixed — the literal path lands at the exact kernel location Alice&#39;s own claim resolves to. Confirmed live, not reasoned about: a real claim + real signature + real HTTP round trip overwrote Alice&#39;s real <code>profile.email</code> with a forged value, using only the root&#39;s own signature. <code>isForeignUsersPrefixWrite()</code> (<code>kernel/manager.ts</code>) now rejects this in both write paths as <code>CANNOT_WRITE_ANOTHER_NAMESPACES_STORAGE</code>.</p><p><strong>Host-owner visibility (§7.1/§7.8) needed two precisions, not assumptions:</strong></p><ul><li><em>&quot;Sees what exists, not its content&quot; is a property of the resolver, not the machine.</em><code>memoriesForPrefix(&quot;&quot;)</code> filtering out <code>users.&lt;label&gt;.&lt;deeper path&gt;</code> is a read-side filter. The underlying storage is one file on one disk; whoever controls that disk can read it directly, outside the resolver entirely. Only genuinely <em>encrypted</em> data (the identity-vault design&#39;s secrets) is actually safe against a host with physical/filesystem access — unencrypted data is not, regardless of what the resolution interface chooses to expose. State this precisely: the host owner sees what exists by design, and could read unencrypted content via physical access — not the same claim as &quot;sees nothing.&quot;</li><li><em>That the host owner sees the list of anchored namespaces at all is a decision, not an accident.</em> It&#39;s information about who uses that machine. Almost certainly the intended shape (§7.1&#39;s whole point), but naming it as a chosen tradeoff rather than a side effect, the same way the public <code>write-head</code> endpoint (§7.8) is named as one.</li></ul><p><strong>Corrected roadmap.</strong> The original §7.4 gap — <code>meshAnnounce.ts</code>&#39;s <code>isNamespaceUsableByIdentity()</code> comparing <code>identityHash</code> (a public, non-secret fingerprint) instead of the signing <code>public_key</code> — is still open and does not depend on <code>.netget.delegates</code> existing, and nothing above blocks it either. Next, in order: (1) the <code>meshAnnounce.ts</code> <code>publicKey</code> fix, (2) the commit-handler multi- namespace head-binding gap (§7.8, tracked live via an <code>it.todo()</code> in <code>commitGate.test.ts</code>, not a silently-green test), if it becomes load-bearing before WS edge nodes ship, (3) <code>.netget.delegates</code> itself, last, once its write surface has nothing else left unverified underneath it.</p><p><strong>Noted, not urgent:</strong> telemetry writing into the same semantic memory chain as user data (rather than its own, separate store) is why the <code>surface.*</code> exclusion was needed in the first place, and makes the global chain grow without bound on every request. Moving telemetry out of the semantic chain entirely would make the exclusion (and this whole class of gap) unnecessary — a real simplification, but a bigger change than this pass attempted.</p><h3 id="_7-10-four-more-questions-checked-before-touching-meshannounce-ts" tabindex="-1">7.10 Four more questions, checked before touching <code>meshAnnounce.ts</code> <a class="header-anchor" href="#_7-10-four-more-questions-checked-before-touching-meshannounce-ts" aria-label="Permalink to &quot;7.10 Four more questions, checked before touching \`meshAnnounce.ts\`&quot;">​</a></h3><p><strong>Status: CLOSED for three; one left as an explicit, unresolved decision (monad commit <code>764ea739</code>).</strong></p><ol><li><strong>The read side, verified live, not assumed safe by symmetry with the write fix.</strong> Does <code>GET users.alice.profile.email</code> through the root host also leak Alice&#39;s data? Traced and tested — it does <strong>not</strong>, today, but only as a side effect of <code>memoryToRow()</code> rewriting a matched row&#39;s own <code>path</code> field (stripping the owning namespace&#39;s prefix) before the branch tree a read builds from it — never a guard written for this purpose. §7.9&#39;s own &quot;sees what exists, not content&quot; claim was correct on this specific point, but by accident, not by design. Made structural instead of incidental: <code>pathResolver.ts</code> now calls the same <code>isForeignUsersPrefixWrite()</code> the write side uses, so this no longer depends on an unrelated function&#39;s internals staying the same for an unrelated reason.</li><li><strong>The guard generalized</strong>, not left as a hardcoded <code>&quot;users.&quot;</code> string. <code>namespaceToKernelPrefix()</code> only ever produces two shapes — <code>&quot;&quot;</code> or <code>&quot;&lt;label&gt;.&lt;prefix&gt;&quot;</code> — so a literal check was a complete enumeration, not a guess, but <code>isForeignUsersPrefixWrite()</code> now builds from the exact same <code>NON_ROOT_KERNEL_PREFIX_LABEL</code> constant <code>namespaceToKernelPrefix()</code> itself uses, so the two structurally cannot drift apart later. Also surfaced a real, separate gap while doing this: <code>commitHandler</code>&#39;s per-event guards (keychain/gateway-authority/netget/surface, and the new forgery guard) never canonicalized paths at all — the same slash-vs-dot bypass already closed on <code>POST /</code> was still open on <code>POST /api/v1/commit</code>. One shared <code>canonicalizeWritePath()</code> (<code>replay.ts</code>) now backs every guard in both files.</li><li><strong>Malformed paths rejected outright.</strong> New <code>isMalformedWritePath()</code> rejects a <code>scheme://</code> prefix, a percent-encoded segment, and any run of 2+ separator characters (<code>..</code>, <code>//</code>, a leading or trailing separator) with <code>400 MALFORMED_WRITE_PATH</code> — checked on the RAW path, deliberately before <code>canonicalizeWritePath()</code>&#39;s own filter step could silently drop the same shape (the actual kernel write path has no such filter; what the underlying <code>.me</code> kernel&#39;s own URI parsing does with a resulting empty segment was unverified, not confirmed safe). Runs before every other guard, in both write paths.</li><li><strong>The one-off test flake got a name, not a shrug.</strong> <code>providerBootRoot.test.ts</code> and <code>requestedNamespace.test.ts</code> shared the literal root namespace <code>&quot;acme.test&quot;</code>, and the latter mutates <code>process.env.ME_NAMESPACE</code> globally in its own <code>beforeAll</code> (restored after, but real shared process state for the window in between) — vitest&#39;s module isolation resets each file&#39;s own module graph, never <code>process.env</code>. Renamed the shared namespace away; confirmed stable across 8 consecutive full-suite runs afterward (0 failures), against roughly 1-in-10 before.</li></ol><p><strong>Left open, on purpose:</strong> what the root claim is allowed to do to <code>groups.*</code> today. Groups live under the shared root namespace itself, which the root claim already legitimately owns — so unlike the <code>users.&lt;label&gt;.*</code> case, the root overwriting or deleting group data with its own signature may be exactly the intended authority relationship, not a bug. The same question as §7.9&#39;s <code>users.*</code> finding, but not the same answer by default; deciding it either way was out of scope for this pass. Related and already tracked: while the multi-namespace-event replay gap (above, <code>it.todo()</code>) stays open, group writes specifically are replayable through it (e.g. re-adding a member who was just removed) — real groups usage raises that gap&#39;s priority, ahead of WS edge nodes, independent of whatever <code>groups.*</code>&#39;s root-authority question resolves to.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./Namespace-Is-Context.html">Namespace-Is-Context.md</a> — §4 (claim ledger) and §5 (anchored vs. context-following), the two ideas this doc combines.</li><li><code>modules/monad/Typescript/src/http/selfMapping.ts</code> — <code>ensureCleakerIdentityConfig()</code>, the already-solid keypair persistence this doc builds on.</li><li><code>modules/monad/Typescript/src/claim/records.ts</code> — <code>claimNamespace()</code>, the mechanism §3 proposes reusing rather than duplicating; <code>materializeProjectedNamespaceClaim()</code>, the live precedent §7.2 generalizes.</li><li><code>modules/netget/Typescript/src/modules/NetGetX/OpenResty/lua/handlers/surface_proxy.lua</code> — the trust-tier code §4 distinguishes from real verification, and where §4&#39;s enforcement would land.</li><li><code>modules/monad/Typescript/src/http/meshAnnounce.ts</code> — <code>isNamespaceUsableByIdentity()</code> (§7.4), already-shipped cross-host claim-conflict checking that §4 did not know existed when first written.</li><li><code>modules/monad/Typescript/src/claim/gatewayAuthority.ts</code> — the vigencia/autorización split §7.3 reuses; its free-standing <code>gatewayId</code> is what §7.3 argues against keeping as a separate ledger.</li><li>Branch <code>migration/main-server-doors</code> (<code>modules/netget/Typescript</code>, commit <code>a7595e3</code>) — already-committed, not-yet-deployed implementation of door-as-pointer (§7.2).</li><li><code>Typescript/typedocs/Operators.md</code> (<code>.me</code> kernel repo) — operator #1, <code>isPointerCall</code>, the primitive §7.2 and §7.5 rest on.</li><li><code>CLAUDE.md</code>&#39;s &quot;Known architectural gaps&quot; — gap #1 and #2, corrected 2026-08-28 to match what this doc found by reading the actual code, rather than the older, now-stale description.</li><li><code>modules/monad/Typescript/src/handlers/commandHandler.ts</code> — <code>rootCommandHandler</code> (§7.7), the generic namespace-write surface with the two real, verified gaps (unclaimed-namespace bypass, no replay/namespace binding) that block implementing <code>.netget.delegates</code> as specified.</li><li><code>modules/monad/Typescript/src/handlers/syncHandler.ts</code> — <code>commitHandler</code> (§7.8), the second write path found to share both gaps, with its own remaining multi-namespace-event limitation.</li><li><code>modules/monad/Typescript/src/claim/netget.ts</code> — <code>isNetgetReservedPath()</code> (§7.8), extracted from <code>commandHandler.ts</code> once <code>commitHandler</code> needed the identical check.</li><li><code>modules/monad/Typescript/src/resources/hostTelemetryLedger.ts</code>, <code>src/resources/usageLedger.ts</code> (§7.8) — the <code>surface.*</code> writers whose interval/per-request traffic into a monad&#39;s own namespace was found to spuriously invalidate that namespace&#39;s own chain head before <code>getNamespaceChainHead()</code> excluded them.</li><li><code>modules/monad/Typescript/tests/commitGate.test.ts</code>, <code>chainHeadWriteAuthorization.test.ts</code>, <code>netgetReservedPathAuthorization.test.ts</code> (§7.8) — the live-verified guarantees and the one test that documents the still-open multi-namespace-event gap on purpose.</li><li><code>modules/monad/Typescript/src/http/internalToken.ts</code> — <code>isSurfaceTelemetryReservedPath()</code> (§7.9), reserving <code>surface.*</code> to internal callers, the same pattern <code>isGatewayRoutingRecordPath()</code> already used for the gateway&#39;s own routing records.</li><li><code>modules/monad/Typescript/src/kernel/manager.ts</code> — <code>isForeignUsersPrefixWrite()</code> (§7.9), the fix for the root-claim-into-<code>users.&lt;other&gt;.*</code> forgery; sits beside <code>isForeignNamespaceCollapsingToRoot()</code>, the mirror-image gap it was written to close.</li><li><code>modules/monad/Typescript/tests/rootWriteDirectionCheck.test.ts</code> (§7.9) — the live-confirmed proof of the forgery, and the regression guard now closing it.</li></ul>`,83)])])}const g=t(i,[["render",n]]);export{m as __pageData,g as default};
